const { Jimp } = require('jimp'); async function run() { const img = await Jimp.read('logo.png'); const w = img.bitmap.width; const h = img.bitmap.height; const s = Math.max(w, h); const bg = new Jimp({width: s, height: s, color: 0x00000000}); bg.composite(img, (s-w)/2, (s-h)/2); bg.resize({w: 512, h: 512}).write('icon-512.png'); bg.resize({w: 192, h: 192}).write('icon-192.png'); } run();
